Развернуть постгрес в докере
docker run -d --name it-camp-postgres -p 5432:5432 -v $HOME/it_camp-postgresql3/data:/var/lib/postgresql/data -e POSTGRES_PASSWORD=123qwe -e POSTGRES_USER=student -e POSTGRES_DB=users  postgres:13

Запуск приложения без импорта uvicorn
uvicorn app:app --reload

Создание скрипта миграции
alembic revision --autogenerate -m ''

Мигрирование
alembic upgrade head

